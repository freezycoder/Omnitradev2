# Hosted runtime seed

These files are deployment snapshots of OmniTrade's generated signal history
and latest real scan caches. They contain no API keys.

On a fresh runtime, `storage.seed.seed_runtime_data` copies them into the active
data directory. Existing runtime data is never replaced.
